using Microsoft.Xna.Framework;

namespace MirageFlow.Shared.Core
{
    // Keeping this simple or empty for now if Game1 handles everything, 
    // or we can migrate Game1 logic to GameApp later if desired.
    public class GameApp 
    {
    }
}
